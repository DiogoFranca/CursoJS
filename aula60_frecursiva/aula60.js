// Função recursiva nada mais é do que uma função que se chama de novo.

// Não, é o contrário, laços são mais rápidos que funções recursivas. Função recursiva é muito usado por quem gosta do paradigma de programação funcional.

// function recursiva(max) {
//     if(max < 0) return;
//     console.log(max);
//     max--;
//     recursiva(max);
// }

// recursiva(10);

















